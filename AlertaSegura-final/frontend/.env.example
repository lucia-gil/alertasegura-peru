# Este proyecto tiene la URL del backend hardcodeada en src/services/api.js
# (http://localhost:8080/api). Si necesitas apuntar a otra URL (por ejemplo
# un backend desplegado), edita esa linea directamente.
#
# Este archivo queda como referencia/documentacion.

VITE_API_URL=http://localhost:8080/api
